apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ my.name }}
spec:
  replicas: {{ config.replicas }}
  revisionHistoryLimit: 1
  selector:
    matchLabels:
      app: {{ my.name }}
  template:
    metadata:
      name: {{ my.name }}
      labels:
        app: {{ my.name }}
      annotations:
        app.kubernetes.io/name: {{ my.name }}
        app.kubernetes.io/version: "{{ config.version }}"
        app.kubernetes.io/component: webservice
        app.kubernetes.io/part-of: {{ config.project }}
        app.kubernetes.io/managed-by: kustomize
        co.elastic.logs/enabled: "true"
        co.elastic.logs/exclude_lines: DEBUG
    spec:
      #restartPolicy: Never
      containers:
      {% for cont in cfg.containers.values() %}
      - name: {{ cont.name | default ("app")}}
        image: {{ config.image_repo }}/{{ app.name }}.app:{{ config.version }}
        imagePullPolicy: {{ cont.imagePullPolicy }}
        envFrom:
        - secretRef:
            name: {{ app.name }}-secrets
        - configMapRef:
            name: {{ app.name }}-vars
        ports:
        - containerPort: {{ cont.ports[0].containerPort }}
          protocol: {{ cont.ports[0].protocol }}
        resources:
          limits:
            cpu: {{ cont.resources.limits.cpu }}
            memory: {{ cont.resources.limits.memory }}
          requests:
            cpu: {{ cont.resources.requests.cpu }}
            memory: {{ cont.resources.requests.memory }}
      {% endfor %}
