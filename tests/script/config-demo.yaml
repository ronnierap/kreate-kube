team: devteam1

deployment:
  version: latest-master
  project: kreate-demo
  replicas: 1
  image_repo: todo.com

  containers:
    app:
      resources:
        limits:
          cpu: 2
        requests:
          cpu: 400m
      startupProbe:
        failureThreshold: 120

servicemonitor:
  endpoints:
    - interval: 20s
      path: /actuator/prometheus


ingress:
  root:
    host: TODO
    port: 8080
  api:
    host: TODO
    port: 8080

service:
  # default service
  ports:
    - port: 8080
      #default name: http
  # extra named services
  http:
    ports:
      - port: 443
        name: https
