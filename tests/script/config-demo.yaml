team: devteam1
env: dev

version: latest-master
project: kreate-demo
replicas: 1
image_repo: todo.com

containers:
  app:
    resources:
      limits:
        cpu: 2
      requests:
        cpu: 400m
    startupProbe:
      failureThreshold: 120
