# This file should just provide basic values, no nested hierarchy

probe_path: /demo/actuator/info
image_repo: https://repo.kisst.org
redis_url: https://redis.kisst.org:6379
service_port: 8080
container_port: 8080

ingress_host_external: api.kisst.org
ingress_host_internal: private.kisst.org

db_egress_cidr: 1.2.3.4/32
#db_egress_port: 8123

redis_egress_cidr: dummy
xyz_egress_cidr: dummy

# do not override cpu_limit
cpu_request: "50m" # extra small for dev environment

serviceaccount_role: some-role-dev
root_whitelist: 10.20.30.40
replicas: 0

ORACLE_URL: localhost:1521
ORACLE_SCHEMA: demo_schema_dev
ORACLE_USR: demo_usr_DEV
DB_USR: dbusrdev
DB_PSW: n0t-v3ry-s3cr3t
secrets:
  DB_USR: mark #a-very-close-comment
  # some comments before
  DB_PSW: n0t-v3ry-s3cr3t  # dekrypted
  # and some comments after
# even some comments at weird indent

  # a comment after a empty line
  REDIS_USR: redis # just another comment
  # some more
    # weird indentation
  REDIS_PSW: n0t-v3ry-s3cr3t  # dekrypted
