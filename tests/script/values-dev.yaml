# This file should just provide basic values, no nested hierarchy

probe_path: /demo/actuator/info
image_repo: https://repo.kisst.org
redis_url: https://redis.kisst.org:6379
service_port: 8080
container_port: 8080

ingress_host_external: api.kisst.org
ingress_host_internal: private.kisst.org

db_egress_cidr: 1.2.3.4/32
#db_egress_port: 8123

redis_egress_cidr: dummy
xyz_egress_cidr: dummy

cpu_limit: "500m" # TODO: use this instead of default?
cpu_request: "100m" # TODO: use this instead of default?

serviceaccount_role: some-role-dev

replicas: 0

env: dev
ORACLE_URL: localhost:1521
ORACLE_SCHEMA: demo_schema_dev
ORACLE_USR: demo_usr_DEV
