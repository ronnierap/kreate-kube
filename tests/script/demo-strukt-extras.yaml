# add some extra config, specifically for strukture call
# that is not needed when all is scripted

# some extra
Deployment:
  main:
    name: demo
    kreate:
      patches: {}

# This entire resource is not in demo-strukt, so will not be generated
PodDisruptionBudget:
  main:
    name: demo-pdb
    kreate:
      name: demo-pdb # TODO: have appname in vals or somehow
    minAvailable: 2

Ingress:
  root:
    annontations:
      nginx.ingress.kubernetes.io/affinity: cookie
      nginx.ingress.kubernetes.io/auth-realm: demo-realm
      nginx.ingress.kubernetes.io/auth-secret: basic-auth
      nginx.ingress.kubernetes.io/auth-type: basic
      nginx.ingress.kubernetes.io/whitelist-source-range: ggg
    labels:
      dummy: jan

Egress:
  redis:
    name: demo-egress-to-redis
  db:
    name: demo-egress-to-db
  xyz:
    name: demo-egress-to-xyz

ConfigMap:
  main:
    name: demo-vars
