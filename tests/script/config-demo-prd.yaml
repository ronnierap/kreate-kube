

# redis has multiple hosts on prod
Egress:
  redis:
    to:
    - cidr: 1.1.1.1/32
      port: 6379
    - cidr: 2.2.2.2/32
      port: 6379
    - cidr: 3.3.3.3/32
      port: 6379

# override threshold that is not a value
HttpProbesPatch:
  main:
    startupProbe:
      failureThreshold: 60
