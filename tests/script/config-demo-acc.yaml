egress:
  redis:
    ignore: True  # redis is local in acc environment

# override threshold that is not a value
containers:
  app:
    startupProbe:
      failureThreshold: 30

vars:
  ORACLE_URL: oracle-acc:1521
  ORACLE_USR: demo_usr_acc
  ORACLE_SCHEMA: demo_schema_acc
