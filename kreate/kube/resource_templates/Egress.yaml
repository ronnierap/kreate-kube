---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ my.name }}
spec:
  podSelector:
    matchLabels:
      egress-to-{{ my.shortname }}: enabled
  policyTypes:
    - Egress
  egress:
    {% if strukt.cidr_list == "allow-all" %}
    - {}
    {% else %}
    {% for port in "{}".format(strukt.port).split(",") %}
    {% for addr in strukt.cidr_list.split(",") %}
    - to:
        - ipBlock:
            cidr: {{ addr }}
      ports:
        - port: {{ port }}
          protocol: TCP
    {% endfor %}
    {% endfor %}
    {% endif %}
